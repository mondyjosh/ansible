- name: "Pre-Tasks | {{ ansible_distribution }} | Update Cache"
  ansible.builtin.dnf:
    update_cache: true

- name: "Flatpak | {{ ansible_distribution }} | Flatpak - Flatpak"
  ansible.builtin.dnf:
    name: flatpak
    state: present
  tags:
    - install
    - flatpak

