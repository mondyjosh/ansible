- name: "Pre-Tasks | Fedora | Update Cache"
  ansible.builtin.dnf:
    update_cache: true
  become: true
  tags:
    - system

- name: "Flatpak | Fedora | Flatpak - Flatpak"
  ansible.builtin.dnf:
    name: flatpak
    state: present
  become: true
  tags:
    - install
    - flatpak
