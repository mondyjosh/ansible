## 2025-01-02: Switching from Flatpak to RPM Module on Fedora for a while to show "So-and-so is playing X" via Steam/Spotify. Will update this role if the change sticks.

- name: "Discord | Install"
  community.general.flatpak:
    name: com.discordapp.Discord
    state: present
  tags:
    - install
    - discord

    # 1. Add RPN-fusion non-free repo
    # 2. dnf update
    # 3. sudo dnf install discord

- name: "Discord | {{ ansible_distribution }} | Install fastfetch"
  become: true
  ansible.builtin.dnf:
    name:
      - discord
  tags:
    - install
    - discord
# sudo dnf install https://download1.rpmfusion.org/nonfree/fedora/rpmfusion-nonfree-release-$(rpm -E %fedora).noarch.rpm
