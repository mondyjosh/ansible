- name: Remove neovim
  shell: rm -rf ~/neovim
  tags:
    - install
    - neovim
    
# TODO: Replace Packer with Lazy setup
# - name: Get Packer
#  ansible.builtin.git:
#    repo: 'https://github.com/wbthomason/packer.nvim'
#    dest: "{{ lookup('env', 'HOME') }}/.local/share/nvim/site/pack/packer/start/packer.nvim"
#  tags:
#    - install
#    - neovim

- name: Clone neovim (nightly)
  ansible.builtin.git:
    repo: "https://github.com/neovim/neovim.git"
    dest: "{{ lookup('env', 'HOME') }}/neovim"
  tags:
    - install
    - neovim
- name: Build neovim (nightly)
  shell: "cd {{ lookup('env', 'HOME') }}/neovim && make CMAKE_BUILD_TYPE=RelWithDebInfo"
  tags:
    - install
    - neovim
- name: Install neovim (nightly)
  become: true
  shell: "cd {{ lookup('env', 'HOME') }}/neovim && make install"
  tags:
    - install
    - neovim

# do I need to do anything about LazyVim?

# do I need this? could this be replaced with LazyVim?
- name: Neovim Plugin Development 1
  become: true
  apt: name=luarocks
  tags:
    - install
    - neovim
    
# do I need this? could this be replaced with LazyVim?
- name: Neovim Plugin Development 2
  become: true
  shell: luarocks install luacheck
  tags:
    - install
    - neovim
